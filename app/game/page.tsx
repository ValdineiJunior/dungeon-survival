"use client";

import GameView from "@/app/components/GameView";

/** Deep link / legacy URL: same combat as root, without hub shell. */
export default function GameRoutePage() {
  return <GameView />;
}
