# Imagens do Hub

Coloque aqui as imagens de fundo para cada área do hub:

- **hub-main.jpg** – tela principal da recepção e áreas que compartilham o mesmo fundo

Se os arquivos não existirem, a página usa um fundo escuro (slate-950) como fallback.
